# AdminLinux Task 1 — Questions

This file lists the task questions for AdminLinux Task 1. Fill in your answers below each question as needed.

1. List three Linux distributions.

2. From the slides, what is the man command used for?

3. What is the difference between rm and rmdir (use the man command to check)?

4. Create the following hierarchy under your home directory, then answer the sub-questions:
   a. Remove dir11 with rmdir in one step. What did you notice? How did you overcome that?
   b. Then remove OldFiles using `rmdir -p`. State what happened to the hierarchy (note: you are in your home directory).
   c. If the output of `pwd` was `/home/user`, write the absolute and relative path for the file `mycv`.

5. Copy the `/etc/passwd` file to your home directory and name it `mypasswd`. Show the command you used.

6. Rename the new file `mypasswd` to `oldpasswd`. Show the command you used.

7. You are in `/usr/bin`. List four different ways to go to your home directory (commands only).

8. List Linux commands in `/usr/bin` that start with the letter `w`. Show the command you used to produce the list.

9. What is the `type` command used for? Provide one example showing output for `ls` and one for `cd`.

10. Show two different command files in `/usr/bin` that start with the letter `c`. Show the command you used to find them.

11. Using the man command (and its options), find commands that can be used to read or view a file. List the commands you found.

12. What is the usage of the `apropos` command? What is it equivalent to?
